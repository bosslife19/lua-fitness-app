import React from 'react';
import { StyleSheet, View } from 'react-native';
import ChatScreen from '../../../../screens/chat/ChatRoom';

const ChatPlace = () => {
    return (
        <>
          <ChatScreen/>  
        </>
    );
}

const styles = StyleSheet.create({})

export default ChatPlace;
